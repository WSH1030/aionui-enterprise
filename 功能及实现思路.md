# 工业智能体平台：功能及实现思路

## 一、总体判断

《工业智能体平台_功能梳理.md》描述的是一个面向制造企业的工业智能体平台目标，不是当前 Rd Worker 已经全部具备的功能清单。

文档中的功能从技术上都可以实现，但需要分阶段开发。当前 Rd Worker 已经具备桌面端、对话、项目、文件工作区、模型配置、Aion Agent、外部 Agent、MCP、Skill 和定时任务等基础能力；企业级登录、部门权限、Token 配额、审计、知识库、业务连接器和统一内网部署仍需要建设。

推荐产品定位：

> Rd Worker 是基于 AionUi/AionCore 改造的企业智能体平台。前期复用已有 Agent 和工具执行能力，逐步建立自己的 Agent Runtime、权限、知识、Skill、审计和企业业务能力。

## 二、总体技术架构

```text
Rd Worker 桌面端
  ├─ 对话和项目界面
  ├─ 文件工作区
  ├─ Skill/专家入口
  ├─ 审批界面
  └─ Token/任务状态展示
          ↓ 企业内网 API
平台服务层
  ├─ 用户登录
  ├─ 部门和角色权限
  ├─ Token 配额
  ├─ 审计日志
  ├─ Skill 管理
  ├─ 知识库管理
  ├─ 工作流管理
  └─ Agent Runtime
          ↓
模型和工具层
  ├─ Ollama + Qwen
  ├─ Codex CLI
  ├─ Aion Agent
  ├─ MCP 工具
  ├─ ERP/MES/PLM/OA
  └─ 飞书/钉钉/企业微信
```

如果公司只要求单机离线使用，可以让所有组件运行在一台电脑上。如果公司需要多人共用项目、知识库、权限和配额，则需要部署企业内网服务器：

```text
Rd Worker 客户端
      ↓ 企业内网
内网平台服务器
      ↓
Ollama/Qwen、数据库、知识库、Agent Runtime
```

“不联网”不等于“没有网络”。企业内部客户端访问内网服务器属于内网通信；真正的安全要求是禁止访问公网模型和未经授权的外部服务。

## 三、企业运行底座（Platform）

| 功能 | 实现思路 | Rd Worker 当前情况 |
|---|---|---|
| 统一工作入口 | Rd Worker 作为统一桌面入口，统一承载 Agent、模型、Skill、项目、工具和任务 | 已有基础 |
| 统一算力管控 | 增加内部模型网关，所有请求先经过网关，再转发到 Ollama、Qwen 或其他内网模型 | 未完成 |
| 数据不出厂 | 默认使用本地文件、本地模型和本地工具；禁止自动切换到公网模型 | 部分具备，完整限制未完成 |
| 按人和部门控制用量 | 建立用户、部门、项目三级 Token 配额，超出后限流或熔断 | 未完成 |
| 全链路审计 | 记录用户、时间、输入、输出、文件、工具、审批、结果和错误 | 未完成 |
| 模型自动路由 | 简单事务使用轻量模型，复杂分析使用强模型，由模型网关统一决定 | 未完成 |
| 私有模型支持 | 统一接入 Ollama、vLLM、LM Studio 等内网模型服务 | Ollama 可用，统一管理未完成 |
| ERP/MES/PLM/OA 对接 | 通过只读连接器或 MCP 获取数据，默认不允许直接写回既有系统 | MCP 基础已有，业务连接器未完成 |
| 协同工具打通 | 由内网消息服务统一转发待办、预警和报告 | 部分配置入口已有，业务闭环未完成 |
| 全栈本地化部署 | 客户端、平台服务、模型、知识库和数据库全部部署在企业内网 | 未完成 |

### 1. 用户、组织和权限

需要建立以下数据模型：

```text
用户 User
部门 Department
角色 Role
权限 Permission
项目 Project
资源 Resource
```

权限判断至少覆盖：

- 用户能否登录；
- 用户能否查看某个项目；
- 用户能否查看某个对话；
- 用户能否读取某个文件夹；
- 用户能否调用某个 Skill；
- 用户能否调用某个模型；
- 用户能否执行命令或修改文件；
- 用户能否查看审计日志；
- 用户能否管理部门和配额。

权限必须在后端校验，不能只在前端隐藏按钮。

### 2. Token 用量管控

每次模型调用前后都记录：

```text
用户 → 部门 → 项目 → Agent → Skill → 模型 → 输入 Token → 输出 Token → 结果
```

支持的限制包括：

- 每人每日/月额度；
- 每部门每日/月额度；
- 单次任务最大 Token；
- 并发任务数量；
- 单个模型的额度；
- 超限后提醒、限流或熔断；
- 管理员查看使用统计。

### 3. 审计日志

审计日志记录：

- 谁发起了任务；
- 使用了哪个项目和文件；
- 使用了哪个 Agent、模型和 Skill；
- Agent 调用了什么工具；
- 工具访问了什么资源；
- 是否经过人工审批；
- 最终结果是什么；
- 是否修改了文件或业务数据。

涉及敏感内容时，需要增加脱敏、日志访问权限和日志保留期限配置。

## 四、企业知识与经验层（Knowledge）

### 1. 企业知识库

知识库处理流程：

```text
文件上传
  → 文档解析
  → 文本切片
  → 向量化
  → 向量库
  → 权限过滤
  → 检索增强生成
  → 返回答案和出处
```

支持的资料包括：

- 制度文件；
- 国家和行业标准；
- 工艺文件；
- 历史项目资料；
- 质量记录；
- 会议纪要；
- 报表和业务说明；
- 企业内部经验总结。

回答必须尽量返回来源文件、版本、页码或段落。没有足够依据时，Agent 必须明确说明不确定，不能编造答案。

### 2. 知识权限和版本

知识库需要支持：

- 按企业、部门、项目、岗位分组；
- 文档版本和有效期；
- 文档负责人；
- 访问权限继承；
- 文档变更重新索引；
- 过期文档自动降低检索优先级或禁止使用；
- 回答时显示实际采用的文档版本。

企业内网版本可以采用 PostgreSQL + pgvector。POC 阶段可以先采用本地文件索引，避免一开始引入过多基础设施。

### 3. 随任务沉淀

用户确认后的内容可以沉淀为：

- 企业知识；
- 业务规则；
- 判例；
- 操作模板；
- Skill；
- 专家 Agent 的长期规则。

沉淀动作必须经过人工确认，避免错误回答自动进入企业知识库。

### 4. 三类知识分层

```text
国家/行业标准
      ↓
行业通用方法论
      ↓
企业制度、工艺和历史经验
```

检索时根据任务类型和用户权限组合使用，企业内部规则优先级高于通用资料，但必须保留来源和版本。

## 五、Skill 与专家智能体层（Skill）

### 1. Skill

一个 Skill 至少包含：

```text
Skill
  ├─ 名称和描述
  ├─ 适用岗位
  ├─ 输入参数
  ├─ 执行步骤
  ├─ 可用模型
  ├─ 可用工具
  ├─ 知识库范围
  ├─ 权限要求
  ├─ 审批规则
  ├─ 版本
  └─ 测试记录
```

Skill 生命周期：

```text
草稿 → 测试 → 审核 → 发布 → 使用 → 反馈 → 迭代/回滚
```

### 2. 专家智能体

为不同岗位配置：

- 系统提示词；
- 岗位规则；
- 专属知识库；
- 允许使用的工具；
- 输出模板；
- 审批要求；
- 结果评价标准。

可以建立：

- 投标专家；
- 报价专家；
- 采购专家；
- 质量专家；
- 工程图纸专家；
- 制造周报专家；
- 合同审核专家；
- 厂长经营助手。

### 3. 企业 Skill 市场

企业内部市场需要支持：

- 按岗位和部门浏览；
- 一键调用；
- 管理员审核；
- 版本控制；
- 权限控制；
- 使用统计；
- 成功率和反馈；
- 下架和回滚。

## 六、Agent Runtime 设计

Agent Runtime 是智能体真正的运行引擎，负责：

- 接收用户任务；
- 拆解任务步骤；
- 管理上下文；
- 调用模型；
- 调用 Skill；
- 读取和修改文件；
- 调用 MCP 或业务连接器；
- 请求人工审批；
- 中断、重试和恢复任务；
- 记录 Token 和审计事件。

建议定义统一的 Agent Runtime 接口，再接入不同执行引擎：

```text
Rd Worker Agent Runtime
  ├─ Aion Agent 适配器
  ├─ Codex app-server 适配器
  ├─ Ollama 直连适配器
  └─ 其他内网 Agent 适配器
```

如果继续使用当前 AionCore 和 ACP 链路，Codex CLI 需要通过 ACP ↔ Codex app-server 适配器接入。Codex CLI 当前使用的是 app-server 协议，不能只增加 `--acp` 参数。

## 七、典型业务场景及实现方法

### 1. 投标报价

实现方法：

- 解析 PDF、Word、Excel 和扫描图片；
- 提取技术要求和商务条款；
- 生成响应清单；
- 生成报价表；
- 标记缺失、矛盾和高风险内容；
- 对图纸和识别不确定项进行人工复核；
- 输出最终报告或报价草稿。

难点是扫描件、图纸、企业报价规则和客户模板差异，需要真实标书样本验证。

文档目标：单次准备从 1–2 天缩短到半小时。该指标必须通过企业真实样本测试后确认，不能只作为宣传值。

### 2. 采购催料

实现方法：

- 读取采购订单、库存、在途物料和交期；
- 计算缺料和停工风险；
- 按交付影响排序；
- 生成催料任务；
- 推送给采购或供应商负责人；
- 处理人工确认和结果反馈。

需要 ERP/MES/采购表格只读连接器。没有实时业务数据时只能做演示，不能产生真实预警。

### 3. 财务三单核销

实现方法：

- 解析采购订单；
- 解析送货单；
- 解析发票；
- 统一供应商、数量、金额、税率和日期字段；
- 自动比对三类单据；
- 标记差异行；
- 生成异常清单；
- 由财务人员确认结果。

这是适合优先做 POC 的场景，因为输入、规则和输出比较明确。

### 4. 来料质检

实现方法：

- 导入质检标准；
- 读取批次和检验数据；
- 按企业规则给出合格、不合格或待复核；
- 自动生成异常单；
- 升级给责任人；
- 保留判定依据和人工修改记录。

具体判定必须以企业质检标准为准，不能让大模型自由决定生产质量结论。

### 5. 工程图纸与 BOM

实现方法：

- 解析 PDF 或图片图纸；
- 识别元器件、规格和数量；
- 读取和拆分 Excel BOM；
- 去重、比对和检查规格冲突；
- 标记缺项和不确定项；
- 输出工程师确认清单。

建议先支持 PDF 图纸和 Excel BOM，再扩展到 CAD 和复杂扫描图纸。这是技术难度较高的场景。

### 6. 企业知识库问答

实现方法：

- 导入制度、工艺和项目资料；
- 按权限检索；
- 生成回答；
- 返回文件名、版本、页码或出处；
- 没有依据时明确说明不确定；
- 支持用户纠正和知识沉淀。

这是最适合 Rd Worker 第一阶段落地的工业场景。

### 7. 日常办公

包括：

- 会议录音转文字；
- 自动生成会议纪要；
- 提取待办、负责人和截止时间；
- 自动生成周报；
- Word、Excel、PPT 处理；
- 合同条款和风险标注；
- 资料归档；
- 消息和待办推送。

Rd Worker 现有文件、对话、工具、定时任务和 MCP 能力可以作为基础，但会议识别、文档模板和企业审批还需要补充。

## 八、面向管理层的企业能力

### 1. AI 资产管理

统一管理：

- 模型；
- Agent；
- Skill；
- 专家规则；
- 知识库；
- 工作流；
- 文档模板；
- 使用记录。

### 2. 合规与审计

实现对对话、文件、模型、工具调用、审批和任务结果的全链路记录，并支持按用户、部门、项目、时间和风险类型查询。

### 3. ESG 报告自动化

从能源、碳排放、废弃物等系统读取数据，自动整理并生成报告初稿，人工终审后导出。

该功能依赖企业数据系统和统一指标定义，不建议放在第一阶段。

### 4. 智能制造成熟度评级

建立 L1–L5 指标体系，记录企业已落地的智能场景、使用频率、量化收益、知识沉淀和治理水平。

### 5. 管理驾驶舱

展示：

- 使用人数；
- 活跃部门；
- Agent/Skill 调用次数；
- 任务成功率；
- 节省时间；
- Token 消耗；
- 风险和审批事件；
- 知识库增长情况。

## 九、数据安全和内网部署

本地模式必须固定使用：

```text
--oss
--local-provider ollama
--model qwen3.5:4b
OLLAMA_HOST=http://127.0.0.1:11434
```

还需要：

- 禁止自动回退到公网模型；
- Ollama 不可用时直接报错；
- 禁止外部 Web 搜索；
- 禁止未经批准的外部 MCP；
- 默认只读权限；
- 文件修改和命令执行需要确认；
- 通过网络白名单或防火墙限制公网访问；
- 统一保存本地或内网日志；
- 模型、文件和对话数据不出企业网络。

仅使用 Ollama 参数不能完全保证所有工具进程不访问网络，严格的不出网要求还需要操作系统或企业网络层的出口限制。

## 十、当前 Rd Worker 的基础和缺口

### 已有或可复用基础

- 桌面端界面；
- 对话和历史记录；
- 项目和文件夹；
- 项目工作目录；
- 文件浏览和预览；
- 模型配置；
- Aion CLI/AionCore 基础能力；
- 外部 Agent 接入框架；
- MCP 工具管理；
- Skill/Assistant 基础；
- 定时任务；
- 部分消息渠道配置；
- 本地后端和数据存储。

### 仍需开发

- 企业级用户登录；
- 部门、角色和资源权限；
- 后端强制权限校验；
- 用户和部门 Token 配额；
- 全链路审计；
- 企业知识库和 RAG；
- Skill 审核、发布和市场；
- 专家 Agent 管理；
- ERP/MES/PLM/OA 连接器；
- 企业工作流和人工审批；
- 完整内网部署；
- Codex CLI 与当前 Agent 协议的稳定桥接；
- 多用户共享数据库和统一管理后台。

注意：项目中即使已经存在登录页面或权限相关代码，也不能直接视为企业功能已经完成。最终必须通过越权测试和管理员操作测试验收。

## 十一、四个月开发规划

### 阶段 0：本周完成 POC

建议只选一个真实场景，不同时实现所有业务。推荐：

```text
本地企业资料问答
  + 项目文件读取
  + Qwen/Ollama 本地模型
  + 输出带出处的分析结果
  + 人工确认后生成报告
```

POC 验收标准：

- 文件不上传外网；
- Qwen 通过 Ollama 工作；
- Agent 能读取指定项目文件夹；
- 能生成结构化结果；
- 能显示来源文件；
- 写文件前需要人工确认；
- 失败时显示明确原因；
- 能记录基本 Token 和任务日志。

### 第 1 个月：平台基础

- Agent Runtime 抽象；
- Ollama/Qwen 接入；
- Aion Agent、Codex Agent 适配层；
- 用户登录；
- 用户、部门和角色模型；
- 项目和任务权限；
- 基础 Token 统计；
- 基础审计日志。

### 第 2 个月：知识和 Skill

- 企业知识库；
- 文档解析和检索；
- 权限过滤；
- 知识版本；
- Skill 创建、测试和发布；
- 专家 Agent 配置；
- 项目记忆。

### 第 3 个月：业务场景

优先选择 1–2 个场景：

- 企业知识库问答；
- 三单核销；
- 投标文件分析；
- 会议纪要和待办；
- 采购缺料分析。

同时补充：

- 人工审批；
- 报告生成；
- 企业消息通知；
- 业务数据只读连接器。

### 第 4 个月：内网部署和治理

- 内网模型服务；
- 内网数据库；
- 统一 Token 配额；
- 管理员后台；
- 审计查询；
- 安全策略；
- 客户端打包；
- 多用户测试；
- 选定场景试运行。

## 十二、最终可行性结论

如果目标是：

```text
四个月完成一个能在公司内部试用的工业智能体平台 MVP
```

这个目标可行。

如果目标是四个月内完整实现文档中的全部业务场景，并达到成熟商业产品水平，则对当前代码基础和单人开发规模来说风险较高。

更合理的目标是：

> 四个月完成平台底座、权限治理、知识库、Skill 系统和 1–2 个真实业务 POC；投标、采购、质检、图纸、ESG 等场景根据企业数据和连接器条件继续扩展。

文档中的功能没有技术上无法实现的内容，但工业业务功能都依赖真实数据、业务规则和人工验收，不能只靠修改聊天界面完成。

## 十三、模型、Aion CLI 与 Agent 编排的分工

不是所有功能都只需要给 Aion CLI 增加一个模型。需要区分模型能力、Agent 执行能力、任务编排能力和企业平台能力。

### 1. 模型加现有 Aion CLI 可以完成的功能

这些功能主要依赖模型、文件工具和现有 Agent 能力：

- 读取项目文件夹；
- 总结文档；
- 生成报告；
- 修改 Word、Excel、PPT；
- 生成会议纪要；
- 普通知识问答；
- 执行简单脚本；
- 基于提示词创建简单 Skill；
- 使用本地 Qwen 和 Ollama 对话。

POC 阶段可以采用：

```text
Rd Worker
  → Aion CLI
    → Ollama
      → Qwen
```

### 2. 需要 Agent 编排能力的功能

下面这些功能不能只依赖模型自由回答，需要设计稳定的任务流程：

- 投标文件解析；
- 三单核销；
- BOM 对比；
- 采购缺料分析；
- 质量判定；
- 自动生成待办；
- 多步骤任务；
- 中途人工确认；
- 失败重试；
- 任务状态追踪；
- 结果校验。

例如三单核销应设计为：

```text
读取采购订单
  → 读取送货单
  → 读取发票
  → 统一字段
  → 程序比对金额和数量
  → 标记差异
  → 人工确认
  → 输出结果
```

模型负责理解、提取和解释；关键计算、比对、权限和最终写入由程序和工作流负责。

### 3. 需要企业平台后端的功能

以下能力不是 Aion CLI 或模型自动提供的，需要 Rd Worker 自己建设平台服务和数据库：

- 用户登录；
- 部门和角色权限；
- 项目隔离；
- 知识库权限；
- Token 配额；
- 管理员管理；
- 审计日志；
- Skill 市场；
- 企业数据连接器；
- ERP/MES/PLM/OA 对接；
- 内网统一部署；
- 多人共享数据；
- 成本统计；
- 企业级安全策略。

### 4. 各部分的职责

```text
模型
= 负责理解和生成内容

Aion CLI
= 负责执行 Agent 任务、调用工具和操作文件

Rd Worker
= 负责界面、项目、对话和用户体验

企业平台后端
= 负责登录、权限、配额、知识库、审计和管理
```

Aion CLI 已经包含一部分 Agent 编排能力，因此不需要一开始重写完整 Agent 引擎。但它不会自动提供企业级权限、Token、审计和业务流程。

### 5. 推荐开发策略

#### 本周 POC

先复用：

```text
Aion CLI + Ollama + Qwen + 现有文件工具/MCP/Skill
```

先验证一个真实场景：

```text
读取企业资料
  → 分析内容
  → 生成报告
  → 人工确认
  → 保存结果
```

#### 后续产品化

再逐步增加 Rd Worker 自己的编排层：

- 任务状态管理；
- 固定业务流程；
- 人工审批；
- 失败重试；
- 结果校验；
- 审计记录；
- Token 统计。

### 6. 最终结论

简单办公能力，使用 Aion CLI 加本地 Qwen 就可以先实现。

稳定的工业业务流程，需要 Agent 编排能力。

企业登录、权限、Token、知识库和审计，需要 Rd Worker 自己开发平台后端。

因此不建议一开始重写 Aion CLI。更稳妥的路线是先使用 Aion CLI 完成 POC，再把真正需要企业化控制的部分逐步建设在 Rd Worker 中。
